import { BagFilterPipe } from './bag-filter.pipe';

describe('BagFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BagFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
